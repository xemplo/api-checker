namespace Xemplo.ApiChecker.Cli;

public static class CliUsage
{
    public const string Text = "Usage: api-checker --old <path-or-url> --new <path-or-url> [--config <path>] [--output text|json] [--rule <RuleId>=<severity> ...]";
}