workflow: GitHubFlow/v1
commit-message-incrementing: MergeMessageOnly

branches:
  main:
    mode: ContinuousDelivery
    label: alpha
    increment: Patch
  release:
    regex: ^release/
    mode: ContinuousDelivery
    label: rc
    increment: None
