workflow: GitHubFlow/v1
commit-message-incrementing: MergeMessageOnly

branches:
  main:
    mode: ContinuousDeployment
    label: alpha
    increment: Patch
  release:
    regex: ^release/
    mode: ContinuousDeployment
    label: rc
    increment: None
